"""Meridian: process mining, diagnosis and business-case analytics over real event logs.

The package is organised by analytical module (discovery, conformance, scoring, roi, network,
simulation) so that each one can be imported, run and tested in isolation rather than only
through the full pipeline.
"""

__version__ = "0.1.0"
